---
title: 'WebSocket เทียบกับ HTTP, Polling และ SSE'
description: 'WebSocket เทียบกับ request/response, short polling, long polling และ Server-Sent Events อย่างไร — latency, overhead และเมื่อไหร่ที่แต่ละแบบเหมาะสม'
sidebar:
  order: 3
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

WebSocket ไม่ใช่วิธีเดียวในการย้ายข้อมูลระหว่างเบราว์เซอร์กับ server และมันก็ไม่ได้เป็นวิธีที่ถูกต้องเสมอไป การจะเลือกได้ดี คุณต้องรู้จักทางเลือกอื่น ๆ บทเรียนนี้จะพาเดินผ่านสี่แนวทางอื่นและจัดมันเรียงเทียบกับ WebSocket บนสองแกนที่สำคัญที่สุด: **latency** (client รู้เรื่องเหตุการณ์ฝั่ง server เร็วแค่ไหน) และ **overhead** (มีงานที่สูญเปล่ามากแค่ไหนต่อข้อความที่มีประโยชน์หนึ่งข้อความ)

## HTTP request/response ธรรมดา

เส้นฐาน client ถาม server ตอบ การแลกเปลี่ยนจบลง server *ไม่มีทาง* พูดก่อนได้ สำหรับการดึงหน้าและส่งฟอร์ม สิ่งนี้สมบูรณ์แบบ แต่สำหรับการรู้เรื่องเหตุการณ์ที่เกิดบน server มันไม่ได้ให้อะไรเลย — client จะต้องถามอีกครั้ง ซึ่งนำเราไปสู่ polling

## Short polling

client ถามว่า "มีอะไรใหม่ไหม?" ตามตัวจับเวลาคงที่ — สมมติทุกไม่กี่วินาที — และ server ตอบทันที โดยส่วนใหญ่ตอบว่า "ไม่มี" เป็นเวลาส่วนใหญ่

<Mermaid title="Short polling: request ซ้ำ ๆ คำตอบส่วนใหญ่ว่างเปล่า" code={pollDiagram} />

export const pollDiagram = `sequenceDiagram
  participant C as Client
  participant S as Server
  loop every few seconds
    C->>S: GET /updates
    S-->>C: 200 (nothing new)
  end
  Note over S: event happens here
  C->>S: GET /updates
  S-->>C: 200 (here is the update)`;

**Latency** ถูกจำกัดด้วยช่วงเวลาของคุณ — เหตุการณ์หนึ่งอาจนั่งรออยู่โดยไม่มีใครเห็นได้นานถึงหนึ่งรอบ polling **Overhead** สูง: request ส่วนใหญ่แบก HTTP header เต็ม ๆ แต่ได้อะไรที่ไม่มีประโยชน์กลับมา กระชับช่วงเวลาให้แน่นขึ้นแล้วคุณตัด latency ลงได้ แต่ก็เพิ่ม request ที่สูญเปล่าเป็นทวีคูณ มันเรียบง่ายและทำงานได้ทุกที่ ซึ่งเป็นคุณงามความดีที่แท้จริงเพียงข้อเดียวของมัน

## Long polling

ตัวแปรที่ฉลาดกว่า client ส่ง request และ server **ถือมันเปิดค้างไว้** จนกว่าจะมีเหตุการณ์เกิดขึ้นหรือถึงเวลา timeout แล้วจึงตอบกลับ client ส่ง request อีกอันทันทีและรอต่ออีกครั้ง

วิธีนี้ดัน latency ลงใกล้เคียงเรียลไทม์ — response มาทันทีที่เหตุการณ์มา — โดยไม่ต้องมีตัวจับเวลาที่วุ่นวาย แต่ทุกข้อความที่ส่งได้ก็ยังเสียค่าใช้จ่ายเป็นรอบ request/response เต็ม ๆ และการเชื่อมต่อใหม่ ดังนั้นภายใต้อัตราเหตุการณ์ที่หนัก overhead จะไต่สูงขึ้น และมันสามารถบีบขีดจำกัดการเชื่อมต่อของ server ได้

## Server-Sent Events (SSE)

SSE เปิดการเชื่อมต่อ HTTP ที่มีอายุยาว **เส้นเดียว** ซึ่ง server สตรีมเหตุการณ์ไปยัง client ตามที่มันเกิดขึ้น มันมีประสิทธิภาพ เชื่อมต่อใหม่อัตโนมัติ และวิ่งบน HTTP ธรรมดา แต่มันถูกออกแบบให้เป็น **ทางเดียว: server → client เท่านั้น** client ส่งข้อมูลกลับผ่านช่องเดียวกันไม่ได้ การจะคุยกับ server มันต้องส่ง HTTP request ธรรมดาแยกต่างหาก

## WebSocket

WebSocket ก็เปิดการเชื่อมต่อเส้นเดียวค้างไว้เช่นกัน แต่มันเป็น **สองทิศทาง**: หลัง handshake ทั้งสองฝ่าย push ข้อความได้อย่างอิสระด้วย overhead ต่อข้อความที่น้อยที่สุด latency ต่ำเท่าที่เครือข่ายจะอนุญาตได้ใน *ทั้งสอง* ทิศทาง และไม่มีภาษี header ต่อข้อความ

## วางเทียบกัน

| แนวทาง | ทิศทาง | Latency ถึง client | Overhead ต่อข้อความ | เหมาะที่สุดเมื่อ |
| --- | --- | --- | --- | --- |
| HTTP request/response | client → server (ครั้งเดียว) | N/A (ไม่มี push) | หนึ่งรอบเต็มต่อ request | หน้าเพจ ฟอร์ม API ธรรมดา |
| Short polling | client ถามซ้ำ ๆ | สูงสุดหนึ่งช่วงเวลา | สูง (ส่วนใหญ่ว่างเปล่า) | การตรวจสอบเล็กน้อยเป็นครั้งคราว |
| Long polling | client ถาม server ถือไว้ | เกือบเรียลไทม์ | หนึ่งรอบต่อเหตุการณ์ | กึ่งเรียลไทม์บน HTTP ธรรมดา |
| SSE | server → client เท่านั้น | เกือบเรียลไทม์ | ต่ำ (หนึ่งสตรีม) | server push ไม่มี uplink จาก client |
| WebSocket | ทั้งสองทิศทาง | ต่ำที่สุด ทั้งสองทาง | ต่ำที่สุด (ข้อความแบบ framed) | เรียลไทม์แบบโต้ตอบสองทาง |

## วิธีเลือก

- ถ้า client เพียงแค่ **อ่าน** สตรีมของเหตุการณ์จาก server เท่านั้น — ฟีดข่าว, การ tail log สด, อัปเดตความคืบหน้า — **SSE** เรียบง่ายกว่าและวิ่งบนโครงสร้างพื้นฐาน HTTP ธรรมดา
- ถ้าทั้งสองฝ่ายต้องส่งบ่อย ๆ และโต้ตอบกัน — แชต, การทำงานร่วมกัน, เกม, การควบคุมแบบสด — **WebSocket** คือตัวเลือกที่เป็นธรรมชาติ
- ถ้าคุณต้องการบางอย่างวันนี้บนโครงสร้างพื้นฐานที่พื้นฐานที่สุดและทราฟฟิกเบา **long polling** เป็นทางออกชั่วคราวที่สมเหตุสมผล ส่วน **short polling** ใช้เฉพาะกรณีที่เล็กน้อยจริง ๆ เท่านั้น

## Common Mistakes

**ใช้ WebSocket สำหรับ Low-Frequency Updates**
อาการ:
- data อัปเดตทุก 30 วินาที แต่ใช้ WebSocket
- เพิ่ม complexity โดยไม่จำเป็น — HTTP polling ทุก 30 วิก็เพียงพอ
- ประเมิน update frequency ก่อนเลือก technology

**ใช้ Long Polling สำหรับ High-Frequency Events**
อาการ:
- ส่ง event 100 ครั้งต่อวินาที ผ่าน long polling
- เปิด HTTP connection ใหม่ 100 ครั้งต่อวินาที — ทำลาย server
- เปลี่ยนไปใช้ WebSocket เมื่อ event rate สูงกว่า 1-2 ครั้งต่อวินาที

> 💡 Real World Example
>
> **Twitter/X:**
> - ใช้ SSE สำหรับ notification และ timeline update (server → client only)
> - WebSocket สำหรับ DM real-time chat (bidirectional)
>
> **Intercom:**
> - เริ่มจาก long polling สำหรับ chat widget
> - migrate ไป WebSocket เมื่อ user load เพิ่มขึ้น — ลด server connection overhead 80%

<Callout title="ความแตกต่างที่ทำให้คนสะดุด">
**SSE เป็น server → client เท่านั้น** **WebSocket เป็นสองทิศทาง** ถ้า client ของคุณไม่จำเป็นต้อง push ผ่านช่องเดียวกันเลย SSE อาจเป็นทั้งหมดที่คุณต้องการ แต่เมื่อใดที่คุณต้องการทราฟฟิกสองทางอย่างแท้จริง ให้เอื้อมไปหา WebSocket
</Callout>

export const quizVs = [
  {
    q: 'อะไรคือข้อจำกัดสำคัญของ Server-Sent Events เมื่อเทียบกับ WebSocket?',
    options: [
      'SSE ไม่สามารถเชื่อมต่อใหม่อัตโนมัติได้',
      'SSE เป็นทางเดียว: server ไป client เท่านั้น',
      'SSE ไม่ทำงานบน HTTP',
      'SSE ไม่สามารถสตรีมข้อความได้',
    ],
    answer: 1,
  },
  {
    q: 'ทำไม short polling ถึงมีแนวโน้มที่จะมี overhead สูง?',
    options: [
      'มันบีบอัดทุก response',
      'request ส่วนใหญ่ไม่ได้คืนข้อมูลใหม่แต่ก็ยังต้องจ่ายค่า request/response เต็ม ๆ',
      'มันเปิด socket นับพันค้างไว้พร้อมกัน',
      'มันเข้ารหัสทุกข้อความสองครั้ง',
    ],
    answer: 1,
  },
  {
    q: 'คุณกำลังสร้างไวต์บอร์ดร่วมกันแบบโต้ตอบที่ผู้ใช้ทุกคนทั้งส่งและรับการแก้ไขอย่างต่อเนื่อง อันไหนเหมาะที่สุด?',
    options: [
      'Short polling',
      'Server-Sent Events',
      'WebSocket',
      'HTTP request/response ธรรมดา',
    ],
    answer: 2,
  },
  {
    q: 'long polling ลด latency เมื่อเทียบกับ short polling ได้อย่างไร?',
    options: [
      'มันส่ง request หลายอันต่อวินาที',
      'server ถือ request เปิดค้างไว้และตอบทันทีที่เหตุการณ์เกิดขึ้น',
      'มันสลับไปใช้ UDP',
      'มันแคช response ไว้ที่ client',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="websocket-foundations/vs-http-polling-sse" questions={quizVs} />

<ProgressTracker client:visible id="websocket-foundations/vs-http-polling-sse" />
